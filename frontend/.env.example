# Backend API GATEWAY base URL (no trailing slash).
# Must point to the API GATEWAY — not just admin-service. Students/Trainers lists
# come from student-service and trainer-service via the gateway.
# Ngrok: use your tunnel URL and ensure ngrok forwards to the GATEWAY (e.g. port 3000).
#   VITE_API_URL=https://your-subdomain.ngrok-free.app
# Local: gateway usually runs on 3000.
#   VITE_API_URL=http://localhost:3000
VITE_API_URL=https://subimbricated-biserrate-narcisa.ngrok-free.dev/
